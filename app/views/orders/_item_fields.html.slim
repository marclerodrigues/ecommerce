div
  =f.hidden_field :_destroy, :id => 'removable'
  =f.label :product
  =f.collection_select :product_id, Product.all, :id, :name, :prompt => "Pick a product"
  =f.label :quantity
  =f.text_field :quantity
  =f.label :amount
  =f.text_field :amount
  =remove_child_button "Remove"